# 🎁 Birthday Surprise Web App

A beautiful, interactive "Gift Box" web experience designed to deliver a personalized birthday surprise. This project uses smooth CSS animations and Vanilla JavaScript to create a memorable "unboxing" moment.

## ✨ Features
* **Interactive Gift Box:** Features a pulsing 3D-shadow effect that invites users to click.
* **Dramatic Countdown:** A 3-second animated transition using the 'Dancing Script' font.
* **Video Reveal:** Automatically plays a personalized video once the countdown hits zero.
* **Magical Effects:** A dynamic "Heart Rain" particle system that generates floating emojis (❤️, 💖, ✨, 🌸).
* **Responsive Design:** Optimized for both mobile and desktop viewing with a soft pastel aesthetic.

## 🛠️ Technical Overview
* **Frontend:** HTML5 & CSS3 (Flexbox, Keyframes).
* **Logic:** Vanilla JavaScript for the timer and particle physics.
* **Fonts:** Hosted via Google Fonts (Quicksand & Dancing Script).

## 🚀 Setup Instructions
1.  **Save the File:** Ensure your main file is named `index.html`.
2.  **Video Configuration:** * The current video source is linked to a Google Drive file.
    * To use your own video, replace the `src` attribute in the `<video>` tag with your direct file link.
3.  **Run:** Open `index.html` in any web browser to view the magic.

## 📂 Customization
You can easily personalize the experience by editing these sections in `index.html`:
* **Message:** Edit the `<h1>` tag for a different name or occasion.
* **Closing Note:** Update the `wish-text` div to change the final message.
* **Colors:** Modify the `background: radial-gradient` in the CSS to match a favorite color.

---
*Created with ❤️ for a special celebration.*
